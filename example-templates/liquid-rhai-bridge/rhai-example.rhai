print("Inside the rhai filter function");

variable::set("liquid_variable_from_rhai", "You can even set values from within the rhai filter function");

"This is the result of running the script as a filter."
